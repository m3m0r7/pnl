parameters:
  level: max
  paths:
    - src/sdk
    - tests
  excludePaths:
    # Golden snapshots of generated code are fixtures, not source to analyse.
    - tests/golden/*
  tmpDir: .phpstan-cache
